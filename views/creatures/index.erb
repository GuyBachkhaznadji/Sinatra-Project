<h2>Resident Creatures</h2>
<section>
 <% @creatures.each do |creature| %>
  <article>
    <p>id: <%=creature.id%></p>
    <p>Name: <%=creature.name%></p>
    <p>Type: <%=creature.type%></p>
    <p>Date of Capture: <%=creature.capture_date%></p>
    <p>Fightable: <%=creature.fight_ready%></p>
    <p>Fighting: <%=creature.gladiator_names%></p>
    <hr/>
  </article>
  <% end %>
</section>



<form method="post" action="/creatures/type/<%=input%>">
  
  <label for="creature-type-field">Creature Types:</label>
  <% @types.each do |type| %>
  <input
  type="radio"
  name="fightable"
  id="creature-type-field"
  value="<%=type%>"
  /><%=type%>
  <% end %>
  <input type="submit" name="input" value="Filter">
</form>